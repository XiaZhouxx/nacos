spring:
  application:
    name: nacos-address
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false